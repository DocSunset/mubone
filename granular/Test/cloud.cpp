#ifdef RUNTESTS
#include "catch.hpp"
#include "../Source/cloud.h"

TEST_CASE("When search distance is 1.0, all references in the audiosphere are potential candidates")
{
}

TEST_CASE("When search distance is 0.0, only the closest reference in the audiosphere is a candidate")
{
}

TEST_CASE("The duration is equivalent to N+1 periods of the trigger oscillator, where N is the overlap factor (from -1 to the number of grains)")
{
}
#endif
